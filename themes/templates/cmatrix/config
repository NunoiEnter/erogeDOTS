# cmatrix config — generated by theme-switch
# Template: themes/templates/cmatrix/config

# Colors: cyan, green, magenta, red, blue, white, yellow, red, etc.
# Can also use: color_green, bold color, etc.

# Asynchronous scroll
asynch = true

# Bold characters
bold = true

# Set custom color (use theme primary via template)
# cmatrix also accepts: green (default), red, blue, white, yellow, magenta, cyan
# We'll use a wrapper script for exact hex colors
