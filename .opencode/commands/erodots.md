---
description: Load erogeDOTS machine context (eroricer skill) for this session
---

Load the `eroricer` skill now and follow its "On load" section: read
`~/erogeDOTS/agent.md` in full, read the last ~10 entries of
`~/erogeDOTS/docs/LOGS.md`, then run `git -C ~/erogeDOTS status --short` and
`git -C ~/erogeDOTS log --oneline -5`.

After loading, report briefly: active theme, clean/dirty tree, most recent log
entry. Then continue with this request: $ARGUMENTS
